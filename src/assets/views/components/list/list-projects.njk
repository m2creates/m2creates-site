<ul class="w-full list">
{%- for post in posts -%}
  <li class="flex flex-col">
    <div class="flex flex-nowrap justify-between gap-x-4 mb-1">
      <a href="{{ post.data.showcase.link }}" target="_blank">{{ post.data.title }}</a>
      <time class="flex-shrink-0" datetime="{{  post.data.datePublish }}" itemProp="datePublished">{{ post.data.datePublish | format('dd LLL yyyy') }}</time>
    </div>
    {% if post.data.tldr %}<p class="mt-0"><em>{{ post.data.tldr | safe }}</em></p>{% endif %}
  </li>
{%- endfor -%}
</ul>