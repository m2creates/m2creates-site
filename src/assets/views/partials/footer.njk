<footer class="w-full pb-8 mx-auto">

  {% include "components/h-card.njk" %}

  <div class="flex flex-col-reverse md:flex-row flex-wrap items-center justify-between">
    <p>&#169; 2011-2022 m2creates &#8226; Melanie E Magdalena</p>
    <nav>
      <ul class="flex gap-4">
        {% for item in navigation.footer %}
        <li class="py-2"><a href="{{ item.href }}">{{ item.text }}</a></li>
        {% endfor %}
      </ul>
    </nav>
  </div>
</footer>
