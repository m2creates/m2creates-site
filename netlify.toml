# Configure Netlify build settings, deploy settings, and environment variables
# @link https://docs.netlify.com/configure-builds/file-based-configuration/

# Settings in the [build] context are global and are applied to all contexts
# unless otherwise overridden by more specific contexts.
[build]

  # Default build command.
  command = "npm run production"

  # Directory that contains the deploy-ready HTML files and assets generated by
  # the build. This is relative to the base directory if one has been set, or the
  # root directory if a base has not been set.
  publish = "dist"

# Deploy Preview context: all deploys generated from a pull/merge request will
# inherit these settings.
[context.deploy-preview.environment]

  # Default build command.
  command = "npm run staging"

  # View draft content on deploy previews
  ELEVENTY_ENV = "staging"